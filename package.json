{
  "name": "dj-architect",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0 --port 5173 --strictPort",
    "dev:idx": "node --input-type=module -e \"import { spawn } from 'node:child_process'; const port = process.env.PORT || '5173'; console.log('[dev:idx] PORT=', port); const vite = new URL('./node_modules/vite/bin/vite.js', import.meta.url).pathname; const p = spawn('node', [vite, '--host','0.0.0.0','--port', String(port), '--strictPort'], { stdio: 'inherit' }); p.on('exit', (c) => process.exit(c ?? 0));\"",
    "build": "vite build",
    "preview": "vite preview --host 0.0.0.0 --port 5173",
    "test": "vitest"
  },
  "dependencies": {
    "idb": "^8.0.3",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "zustand": "^5.0.9"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.2.1",
    "jest": "^30.2.0",
    "vite": "^5.2.0",
    "vitest": "^4.0.17"
  }
}
